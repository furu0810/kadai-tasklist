<h1>id:
    <%= @task.id %>
    のタスクの編集ページ
</h1>

    <%= form_with(model: @task, local: true) do |f| %>
    
    <%= f.label :content, "タスク内容" %>
    
    <%= f.text_field :content %>
    
    <%= f.submit "追加" %>
    
    <% end %>
    
    <%= link_to "トップページに戻る", tasks_path %>
    
    
     