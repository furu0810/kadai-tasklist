<h1>
  id: 
  <%= @task.id %>
  のタスクの詳細な内容
</h1>

  <table class="table table-bordered">
    <thead>
      <tr>
        <th class="text-center">id</th>
        <th class="text-center">ステータス</th>
        <th class="text-center">タスク内容</th>
      </tr>
    
    <tbody>
      <tr>
      <td class="text-center">
        <%= @task.id %>
      </td>
  
      <td class="text-center">
        <%= @task.status %>
      </td>
    
      <td class="text-center">
        <%= @task.content %>
      </td>
      </tr>
    </tbody>
  </table>  
  
    <%= link_to "トップページに戻る", tasks_path, class: "btn btn-primary" %>
    
    <%= link_to "このタスク内容を編集する", edit_task_path(@task), class: "btn btn-secondary" %>
    
    <%= link_to "このタスク内容を削除する", @task, method: :delete, data: { confirm: "本当に削除してよろしいですか?"}, class: "btn btn-danger" %>